name: app_badger_example
description: "Demonstrates how to use the app_badger plugin."

publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.7.2 <4.0.0"  # Ensure Dart SDK compatibility

dependencies:
  flutter:
    sdk: flutter
  awesome_notifications: any
  app_badger:
    path: ../
  cupertino_icons: ^1.0.8  # Ensure null safety

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # For better linting and code quality

flutter:
  uses-material-design: true  # Ensures Material Design icons are included in the app
  assets: