name: app_badger_example
description: "Demonstrates how to use the app_badger plugin."
version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"  # Ensure Dart SDK compatibility

dependencies:
  flutter:
    sdk: flutter
  app_badger:
    path: ../
  cupertino_icons: ^1.0.8
  meta: ^1.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true